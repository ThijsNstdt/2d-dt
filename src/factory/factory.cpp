//// Created by thijs on 14/12/2022.//#include "factory.hpp"Factory::InitGeometry Factory::strToInitGeom(const std::string &string) {    if (string == "tetrahedron") {        return Tetrahedron;    }    if (string == "growth") {        return Growth;    }    else {        throw std::runtime_error("In `strToInitGeom` of Factory: input " + string +  " is not supported");    }}std::string Factory::initGeomToStr(const Factory::InitGeometry &geom) {    if (geom == Tetrahedron) {        return "tetrahedron";    }    if (geom == Growth) {        return "growth";    }    else {        throw std::runtime_error("In `initGeomToStr` of Factory: input geom is not supported");    }}